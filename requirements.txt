# requirements.txt for CS441-Final-Project
# This file lists the project's Python dependencies. Collaborators should
# activate the project's virtual environment and run `pip install -r requirements.txt`.
#
# It's recommended to pin versions after the team stabilizes dependencies, e.g.
# numpy==1.26.0
# pandas==2.2.0
# scikit-learn==1.3.2
# streamlit==1.25.0
#
# To update the pinned list locally after installing packages:
#   pip freeze > requirements.txt

numpy
pandas
scikit-learn
streamlit
